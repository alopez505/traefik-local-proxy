# Certificates

This directory holds local, generated TLS assets used by Traefik during
development. Only this file is tracked in Git; everything else here is
generated by mkcert and gitignored.

For generation, rotation, and trust-store setup, see
[../docs/onboarding/certificates.md](../docs/onboarding/certificates.md).
